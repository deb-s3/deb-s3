#!/usr/bin/env bash
#MISE description="Build gem"
if [ "${GITHUB_REF_TYPE}" == "tag" ]; then
    version="${GITHUB_REF_NAME}"
else
    version="$(git describe --tags)"
fi
sed -i 's,VERSION = .*,VERSION = "'${version}'",' lib/deb/s3.rb
gem build deb-s3.gemspec
git checkout lib/deb/s3.rb
